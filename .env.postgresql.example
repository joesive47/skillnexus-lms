# ตัวอย่าง DATABASE_URL สำหรับ PostgreSQL
# แก้ไขค่าต่อไปนี้ให้ตรงกับ PostgreSQL ของคุณ:
# - username: ชื่อผู้ใช้ (default: postgres)
# - password: รหัสผ่าน
# - host: localhost หรือ IP address
# - port: 5432 (default)
# - database: ชื่อฐานข้อมูล (skillnexus)

DATABASE_URL="postgresql://postgres:password@localhost:5432/skillnexus?schema=public"

# ตัวอย่างอื่นๆ:
# DATABASE_URL="postgresql://myuser:mypassword@localhost:5432/skillnexus?schema=public"
# DATABASE_URL="postgresql://postgres:admin123@192.168.1.100:5432/skillnexus?schema=public"
