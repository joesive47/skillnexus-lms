# 🔧 คู่มือแก้ไขปัญหา SkillNexus

## 🚀 เริ่มต้นใช้งานอย่างรวดเร็ว

```bash
npm run quick-start
```

คำสั่งนี้จะ:
- ตรวจสอบและสร้างไฟล์ .env
- ติดตั้ง dependencies
- ตั้งค่าฐานข้อมูล
- สร้างข้อมูลตัวอย่าง
- เริ่มเซิร์ฟเวอร์

## 🔍 การตรวจสอบปัญหา

### 1. ตรวจสอบระบบผ่าน Command Line
```bash
npm run debug:system
```

### 2. ตรวจสอบระบบผ่าน Web UI
เข้าไปที่: http://localhost:3000/debug

### 3. รีเซตระบบ
```bash
npm run reset:system
```

## ❌ ปัญหาที่พบบ่อย

### 1. Database Connection Error
**อาการ:** ไม่สามารถเชื่อมต่อฐานข้อมูลได้

**วิธีแก้:**
```bash
# ตรวจสอบไฟล์ database
ls -la prisma/dev.db

# สร้างฐานข้อมูลใหม่
npm run db:push

# รีเซตข้อมูล
npm run reset:system
```

### 2. Authentication Error
**อาการ:** ไม่สามารถเข้าสู่ระบบได้

**วิธีแก้:**
```bash
# ตรวจสอบ environment variables
cat .env | grep AUTH

# รีเซตข้อมูล users
npm run reset:system
```

**ข้อมูลการเข้าสู่ระบบ:**
- Admin: `admin@skillnexus.com` / `admin123`
- Test User: `test@skillnexus.com` / `test123`

### 3. Missing Dependencies
**อาการ:** Module not found errors

**วิธีแก้:**
```bash
# ลบ node_modules และติดตั้งใหม่
rm -rf node_modules package-lock.json
npm install

# หรือใช้ quick-start
npm run quick-start
```

### 4. Prisma Client Error
**อาการ:** Prisma client is not generated

**วิธีแก้:**
```bash
npm run db:generate
npm run db:push
```

### 5. Port Already in Use
**อาการ:** Port 3000 is already in use

**วิธีแก้:**
```bash
# หา process ที่ใช้ port 3000
netstat -ano | findstr :3000

# Kill process (Windows)
taskkill /PID <PID> /F

# หรือใช้ port อื่น
PORT=3001 npm run dev
```

## 🛠️ คำสั่งที่มีประโยชน์

### Development
```bash
npm run dev              # เริ่มเซิร์ฟเวอร์ development
npm run build            # Build สำหรับ production
npm run start            # เริ่มเซิร์ฟเวอร์ production
```

### Database
```bash
npm run db:generate      # Generate Prisma client
npm run db:push          # Push schema ไปยัง database
npm run db:migrate       # สร้าง migration
npm run db:studio        # เปิด Prisma Studio
npm run db:seed          # Seed ข้อมูลตัวอย่าง
```

### System Management
```bash
npm run reset:system     # รีเซตระบบทั้งหมด
npm run debug:system     # ตรวจสอบสถานะระบบ
npm run quick-start      # ตั้งค่าระบบอัตโนมัติ
```

### Testing
```bash
npm run test             # รัน tests
npm run test:watch       # รัน tests แบบ watch mode
npm run test:coverage    # รัน tests พร้อม coverage
```

## 📋 Checklist การแก้ไขปัญหา

- [ ] ตรวจสอบไฟล์ `.env` มีครบถ้วน
- [ ] ตรวจสอบ `node_modules` ติดตั้งแล้ว
- [ ] ตรวจสอบฐานข้อมูล `prisma/dev.db` มีอยู่
- [ ] ตรวจสอบ Prisma client generate แล้ว
- [ ] ตรวจสอบมีข้อมูล users ในระบบ
- [ ] ตรวจสอบ port 3000 ว่าง
- [ ] ตรวจสอบ console logs สำหรับ errors

## 🆘 ขอความช่วยเหลือ

หากยังแก้ไขปัญหาไม่ได้:

1. เข้าไปที่ http://localhost:3000/debug
2. ตรวจสอบ browser console (F12)
3. ตรวจสอบ terminal logs
4. รัน `npm run debug:system` และส่งผลลัพธ์

## 🔄 การรีเซตระบบทั้งหมด

หากต้องการเริ่มต้นใหม่ทั้งหมด:

```bash
# ลบข้อมูลทั้งหมด
rm -rf node_modules package-lock.json prisma/dev.db .next

# เริ่มต้นใหม่
npm run quick-start
```