import { SafeWrapper } from '@/components/safe-wrapper';
import SafeLandingPage from '@/components/safe-landing-page';

export default function LandingPage() {
  return (
    <SafeWrapper>
      <SafeLandingPage />
    </SafeWrapper>
  );
}
