# แสดงข้อที่ตอบผิดในหน้าผลลัพธ์ ✅

## 🎯 ฟีเจอร์ใหม่

หน้าแสดงผลจะแสดง:
1. ✅ คะแนนที่ได้
2. ✅ จำนวนข้อถูก/ผิด
3. ✅ **รายการข้อที่ตอบผิด พร้อมคำตอบที่ถูกต้อง**

## 📊 ตัวอย่างการแสดงผล

```
┌─────────────────────────────────────────────┐
│         🏆 คะแนนของคุณ: 80%                │
│         ✅ ผ่าน (Intermediate)              │
│                                             │
│  ตอบถูก: 8/10 ข้อ                          │
│  คะแนน: 40/50 คะแนน                        │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ ❌ ข้อที่ตอบผิด (2 ข้อ)                    │
├─────────────────────────────────────────────┤
│ 📌 ข้อ 3  🏷️ JavaScript                    │
│                                             │
│ JavaScript คืออะไร?                         │
│                                             │
│ ❌ คำตอบของคุณ:                            │
│    ภาษา HTML                                │
│                                             │
│ ✓ คำตอบที่ถูกต้อง:                         │
│    ภาษา Programming                         │
├─────────────────────────────────────────────┤
│ 📌 ข้อ 7  🏷️ React                         │
│                                             │
│ React ใช้สำหรับอะไร?                        │
│                                             │
│ ❌ คำตอบของคุณ:                            │
│    Backend Development                      │
│                                             │
│ ✓ คำตอบที่ถูกต้อง:                         │
│    Frontend Development                     │
└─────────────────────────────────────────────┘
```

## 🔧 Technical Implementation

### 1. เก็บข้อที่ตอบผิดใน sessionStorage

```typescript
// assessment/[careerId]/page.tsx
const wrongQuestions: any[] = []

questions.forEach((question, index) => {
  const isCorrect = userAnswer === correctAnswer
  
  if (!isCorrect) {
    wrongQuestions.push({
      number: index + 1,
      question: question.questionText,
      userAnswerText: question[`option${userAnswer + 1}`],
      correctAnswerText: question[`option${correctAnswer + 1}`],
      skill: question.skillName
    })
  }
})

sessionStorage.setItem('wrongQuestions', JSON.stringify(wrongQuestions))
```

### 2. แสดงผลในหน้า Results

```typescript
// results/page.tsx
const [wrongQuestions, setWrongQuestions] = useState<any[]>([])

useEffect(() => {
  const stored = sessionStorage.getItem('wrongQuestions')
  if (stored) {
    setWrongQuestions(JSON.parse(stored))
  }
}, [])
```

### 3. UI Component

```tsx
{wrongQuestions.length > 0 && (
  <Card className="border-red-200">
    <CardHeader>
      <CardTitle className="text-red-600">
        ข้อที่ตอบผิด ({wrongQuestions.length} ข้อ)
      </CardTitle>
    </CardHeader>
    <CardContent>
      {wrongQuestions.map((item) => (
        <div className="bg-red-50 p-4 rounded-lg">
          <Badge variant="destructive">ข้อ {item.number}</Badge>
          <p className="font-medium">{item.question}</p>
          
          <div className="text-red-600">
            ❌ คำตอบของคุณ: {item.userAnswerText}
          </div>
          
          <div className="text-green-600">
            ✓ คำตอบที่ถูกต้อง: {item.correctAnswerText}
          </div>
        </div>
      ))}
    </CardContent>
  </Card>
)}
```

## 📝 ข้อมูลที่แสดง

สำหรับแต่ละข้อที่ตอบผิด:
- ✅ หมายเลขข้อ (เช่น ข้อ 3)
- ✅ ทักษะที่เกี่ยวข้อง (เช่น JavaScript)
- ✅ โจทย์คำถาม
- ✅ คำตอบที่ผู้ใช้เลือก (สีแดง ❌)
- ✅ คำตอบที่ถูกต้อง (สีเขียว ✓)

## 🎨 Design

- **สีแดง**: ข้อที่ตอบผิด, คำตอบของผู้ใช้
- **สีเขียว**: คำตอบที่ถูกต้อง
- **Badge**: แสดงหมายเลขข้อและทักษะ
- **Background**: พื้นหลังสีแดงอ่อน (red-50)

## ✅ Benefits

1. **เรียนรู้จากความผิดพลาด** - เห็นคำตอบที่ถูกทันที
2. **ทบทวนง่าย** - รู้ว่าข้อไหนผิด ผิดตรงไหน
3. **ปรับปรุงทักษะ** - รู้จุดอ่อนที่ต้องพัฒนา
4. **UX ดีขึ้น** - ไม่ต้องจำว่าตอบอะไรไป

## 🚀 Deployment

```bash
✅ Committed และ Pushed
✅ Vercel auto-deploy
✅ Live: https://www.uppowerskill.com
```

## 📁 Files Changed

1. `src/app/skills-assessment/assessment/[careerId]/page.tsx`
   - เก็บข้อที่ตอบผิดใน sessionStorage

2. `src/app/skills-assessment/results/page.tsx`
   - แสดงข้อที่ตอบผิดพร้อมคำตอบที่ถูก

---

**Status:** ✅ Completed  
**Version:** 2.1
