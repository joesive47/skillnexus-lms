# 🤖 คู่มือการใช้งาน RAG Chatbot - SkillNexus LMS

## ✅ ปัญหาที่แก้ไขแล้ว

### 🔧 การรองรับไฟล์ .docx
- ✅ **แก้ไขแล้ว**: ระบบสามารถประมวลผลไฟล์ .docx ได้แล้ว
- ✅ **เพิ่ม**: การรองรับไฟล์ .doc, .pdf, .xlsx, .txt
- ✅ **ปรับปรุง**: การจัดการ error และ logging ที่ดีขึ้น

### 🎯 ไฟล์ที่รองรับ
| ประเภทไฟล์ | นามสกุล | สถานะ |
|------------|---------|-------|
| PDF | .pdf | ✅ รองรับ |
| Word | .docx, .doc | ✅ รองรับ |
| Excel | .xlsx, .xls | ✅ รองรับ |
| Text | .txt | ✅ รองรับ |

## 🚀 วิธีการใช้งาน

### 1. การอัพโหลดเอกสาร
```bash
# เข้าสู่ระบบในฐานะ Admin หรือ Teacher
# ไปที่หน้า Dashboard > RAG Management
# หรือใช้ Document Upload Component
```

### 2. การทดสอบระบบ
```bash
# รันสคริปต์ทดสอบ
npx tsx scripts/clean-and-test-docx.mjs

# ทดสอบการอัพโหลดไฟล์
npx tsx scripts/test-rag-fix.mjs
```

### 3. การใช้งาน Chatbot
- เปิด Chatbot Widget (ปุ่มสีน้ำเงินมุมขวาล่าง)
- พิมพ์คำถามเกี่ยวกับเนื้อหาที่อัพโหลดไว้
- ระบบจะค้นหาและตอบจากเอกสารที่เกี่ยวข้อง

## 🔍 ตัวอย่างการใช้งาน

### คำถามที่ทดสอบแล้ว
- "SCORM คืออะไร"
- "SkillNexus รองรับ SCORM เวอร์ชันไหนบ้าง"
- "ฟีเจอร์ SCORM ที่รองรับมีอะไรบ้าง"
- "วิธีอัพโหลด SCORM ทำอย่างไร"

### ผลลัพธ์ที่คาดหวัง
```
ตามข้อมูลที่พบ:

[1] SCORM (Sharable Content Object Reference Model) เป็นมาตรฐานสำหรับการสร้างและแบ่งปันเนื้อหาการเรียนรู้ออนไลน์...

แหล่งข้อมูล: Knowledge Base.txt
```

## 🛠️ การแก้ไขปัญหา

### ปัญหา: "Unsupported file type for text extraction"
**สาเหตุ**: ไฟล์ .docx ไม่สามารถประมวลผลได้

**วิธีแก้ไข**: ✅ แก้ไขแล้ว
- เพิ่มการรองรับ MIME type `application/octet-stream`
- ปรับปรุง document-processor.ts
- เพิ่ม error handling ที่ดีขึ้น

### ปัญหา: Chatbot ตอบไม่ได้
**สาเหตุ**: ไม่มีข้อมูลในระบบ RAG

**วิธีแก้ไข**:
1. อัพโหลดเอกสารใหม่ผ่าน RAG Management
2. ตรวจสอบว่าไฟล์ถูกประมวลผลสำเร็จ
3. รันสคริปต์ทดสอบ

## 📊 สถิติการทำงาน

### ผลการทดสอบล่าสุด
- ✅ การประมวลผลไฟล์: สำเร็จ
- ✅ การสร้าง embeddings: สำเร็จ
- ✅ การค้นหาข้อมูล: สำเร็จ (33.73% similarity)
- ✅ การตอบคำถาม: สำเร็จ

### ประสิทธิภาพ
- **Chunk Size**: 1000 ตัวอักษร
- **Overlap**: 200 ตัวอักษร
- **Max Results**: 3 ผลลัพธ์
- **Similarity Threshold**: 0.2 (20%)

## 🔧 การตั้งค่าขั้นสูง

### Environment Variables
```env
RAG_CHUNK_SIZE=1000
RAG_CHUNK_OVERLAP=200
RAG_MAX_RESULTS=3
```

### การปรับแต่ง Similarity
```typescript
// ใน rag-service.ts
const combinedSimilarity = (semanticSimilarity * 0.7) + (textSimilarity * 0.3)
```

## 📝 API Endpoints

### อัพโหลดเอกสาร
```
POST /api/chatbot/upload-document
Content-Type: multipart/form-data

Body:
- file: File (required)
- courseId: string (optional)
```

### ถามคำถาม Chatbot
```
POST /api/chatbot
Content-Type: application/json

Body:
{
  "message": "คำถามของคุณ",
  "sessionId": "unique-session-id"
}
```

## 🎉 สรุป

### ✅ สิ่งที่ทำงานได้แล้ว
- การประมวลผลไฟล์ .docx, .pdf, .xlsx, .txt
- ระบบ RAG ที่ตอบคำถามได้
- การค้นหาด้วย semantic similarity
- Chatbot widget ที่ใช้งานได้

### 🚀 ขั้นตอนถัดไป
- เพิ่มการรองรับไฟล์ประเภทอื่น
- ปรับปรุงความแม่นยำของการตอบคำถาม
- เพิ่มการจัดการ context ที่ดีขึ้น
- สร้าง admin interface สำหรับจัดการเอกสาร

---

**หมายเหตุ**: ระบบได้รับการทดสอบและทำงานได้ปกติแล้ว ผู้ใช้สามารถอัพโหลดไฟล์ .docx และใช้งาน chatbot ได้ทันที