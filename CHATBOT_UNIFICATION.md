# 🤖 Chatbot Widget Unification

## การรวม Chatbot Widget เป็นตัวเดียว

### ปัญหาเดิม
- มี Chatbot Widget 3 ตัวที่ทำงานซ้ำซ้อนกัน:
  1. `ChatWidget` - Basic chatbot
  2. `ChatbotWidget` - Knowledge-based chatbot  
  3. `AIVirtualAssistant` - AI assistant

- ใน `layout.tsx` มีการใช้งาน 2 ตัวพร้อมกัน ทำให้เกิดการซ้ำซ้อน
- ประสิทธิภาพไม่ดี เนื่องจากมี component หลายตัวทำงานพร้อมกัน

### การแก้ไข

#### 1. สร้าง UnifiedChatWidget
- รวมฟีเจอร์ที่ดีที่สุดจากทั้ง 3 ตัว
- ปรับปรุงประสิทธิภาพและ UX
- เพิ่มฟีเจอร์ minimize/maximize
- รองรับ dark mode

#### 2. สร้าง useChat Hook
- แยก logic การจัดการ chat state ออกมา
- รองรับ request cancellation
- Optimized API calls with fallback

#### 3. ปรับปรุงประสิทธิภาพ
- ใช้ `useCallback` และ `useMemo` เพื่อลด re-renders
- Auto-focus input เมื่อเปิด chat
- Smooth scrolling และ animations
- Request cancellation เมื่อส่งข้อความใหม่

### ฟีเจอร์หลัก

#### 🎯 Core Features
- **Unified Interface** - หน้าตาเดียวที่รวมฟีเจอร์ทั้งหมด
- **Smart API Fallback** - ลอง knowledge-search ก่อน แล้วค่อย fallback ไป basic API
- **Minimize/Maximize** - สามารถย่อ/ขยายหน้าต่างได้
- **Auto-scroll** - เลื่อนไปข้อความล่าสุดอัตโนมัติ
- **Request Cancellation** - ยกเลิก request เก่าเมื่อส่งใหม่

#### 🎨 UI/UX Improvements
- **Responsive Design** - ปรับขนาดตามหน้าจอ
- **Dark Mode Support** - รองรับธีมมืด
- **Loading Indicators** - แสดงสถานะการโหลดที่สวยงาม
- **Accessibility** - เพิ่ม aria-labels และ keyboard navigation
- **Smooth Animations** - เอฟเฟกต์การเคลื่อนไหวที่นุ่มนวล

#### ⚡ Performance Optimizations
- **Memoized Components** - ลด re-renders ที่ไม่จำเป็น
- **Efficient State Management** - ใช้ custom hook
- **Request Optimization** - ยกเลิก request ที่ไม่ต้องการ
- **Lazy Loading** - โหลดข้อความเริ่มต้นเมื่อเปิดเท่านั้น

### การใช้งาน

#### ในไฟล์ Layout
```tsx
import UnifiedChatWidget from '@/components/chatbot/UnifiedChatWidget'

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="th">
      <body>
        {children}
        <UnifiedChatWidget />
      </body>
    </html>
  )
}
```

#### การใช้ useChat Hook
```tsx
import { useChat } from '@/hooks/useChat'

function MyComponent() {
  const { messages, isLoading, sendMessage, addMessage } = useChat()
  
  const handleSend = async () => {
    await sendMessage('Hello!')
  }
  
  return (
    // Your component JSX
  )
}
```

### API Endpoints ที่ใช้

1. **Primary**: `/api/chatbot/knowledge-search`
   - ใช้ RAG (Retrieval-Augmented Generation)
   - ให้ความแม่นยำและแหล่งข้อมูล

2. **Fallback**: `/api/chatbot`
   - Basic chatbot API
   - ใช้เมื่อ knowledge-search ไม่สามารถใช้งานได้

### ไฟล์ที่เปลี่ยนแปลง

#### ไฟล์ใหม่
- `src/components/chatbot/UnifiedChatWidget.tsx` - Main unified widget
- `src/hooks/useChat.ts` - Chat state management hook
- `CHATBOT_UNIFICATION.md` - เอกสารนี้

#### ไฟล์ที่แก้ไข
- `src/app/layout.tsx` - เปลี่ยนจากใช้ 2 widgets เป็น 1 widget

#### ไฟล์เก่าที่ยังคงอยู่ (สำหรับ backward compatibility)
- `src/components/chatbot/ChatWidget.tsx`
- `src/components/chatbot/chatbot-widget.tsx`
- `src/components/ai/AIVirtualAssistant.tsx`

### การทดสอบ

1. **เปิดเว็บไซต์** - ควรเห็น chatbot button ตัวเดียวที่มุมขวาล่าง
2. **คลิกเปิด chat** - ควรแสดงข้อความต้อนรับ
3. **ส่งข้อความ** - ควรได้รับการตอบกลับจาก AI
4. **ทดสอบ minimize** - คลิกปุ่ม minimize ควรย่อหน้าต่าง
5. **ทดสอบ dark mode** - เปลี่ยนธีมควรเปลี่ยนสีตาม

### ประโยชน์ที่ได้รับ

✅ **ลดความซ้ำซ้อน** - จาก 3 widgets เหลือ 1 widget  
✅ **ปรับปรุงประสิทธิภาพ** - ลด memory usage และ re-renders  
✅ **UX ที่ดีขึ้น** - ฟีเจอร์ minimize/maximize และ animations  
✅ **Maintainability** - โค้ดที่จัดระเบียบและแยก logic ออกมา  
✅ **Accessibility** - รองรับ screen readers และ keyboard navigation  
✅ **Dark Mode** - รองรับธีมมืดอย่างสมบูรณ์  

### การพัฒนาต่อ

1. **เพิ่ม Chat History** - บันทึกประวัติการสนทนา
2. **Voice Input** - รองรับการพูดแทนการพิมพ์
3. **File Upload** - อัปโหลดไฟล์เพื่อถามคำถาม
4. **Multi-language** - รองรับหลายภาษา
5. **Chat Analytics** - วิเคราะห์การใช้งาน chatbot