# 🤖 Chatbot Knowledge Base Fix Guide

## ปัญหาที่พบ
Chatbot ไม่ได้ตอบคำถามตามข้อมูลจริงที่มีในระบบ แต่ตอบแบบทั่วไป ๆ เหมือนเดิม โดยเฉพาะคำถามเกี่ยวกับ:
- SCORM Support
- PWA (Progressive Web App) 
- ราคาหลักสูตร
- ใบประกาศนียบัตร

## สาเหตุของปัญหา
1. **ข้อมูลใน Knowledge Base.txt ไม่ได้ถูกนำเข้าสู่ระบบ RAG**
2. **ไม่มีข้อมูลใน chatKnowledgeBase table**
3. **SmartChatbot ไม่ได้ค้นหาข้อมูลจาก Knowledge Base ก่อนใช้ RAG**

## วิธีแก้ไข

### 1. รันสคริปต์แก้ไขอัตโนมัติ
```bash
# รันสคริปต์แก้ไขทั้งหมด
scripts/fix-chatbot-knowledge.bat
```

### 2. รันแต่ละขั้นตอนแยก

#### ขั้นตอนที่ 1: เพิ่มข้อมูลใน Knowledge Base
```bash
node scripts/seed-scorm-knowledge.mjs
```

#### ขั้นตอนที่ 2: นำเข้าข้อมูลเข้าสู่ระบบ RAG
```bash
node scripts/import-knowledge-base.mjs
```

### 3. ทดสอบระบบ
```bash
node scripts/test-chatbot-fixed.mjs
```

## การปรับปรุงที่ทำ

### 1. เพิ่มข้อมูลใน Knowledge Base
- ✅ ข้อมูลเกี่ยวกับ SCORM (คืออะไร, เวอร์ชันที่รองรับ, ฟีเจอร์)
- ✅ ข้อมูลเกี่ยวกับ PWA (Progressive Web App)
- ✅ ข้อมูลเกี่ยวกับราคาหลักสูตร
- ✅ ข้อมูลเกี่ยวกับใบประกาศนียบัตร

### 2. ปรับปรุง SmartChatbot
- ✅ เพิ่ม `findDirectMatch()` เพื่อค้นหาจาก Knowledge Base ก่อน
- ✅ ปรับปรุง fuzzy matching ให้แม่นยำขึ้น
- ✅ เพิ่ม topics สำหรับ SCORM และ PWA

### 3. ปรับปรุงระบบ RAG
- ✅ นำเข้าข้อมูลจาก Knowledge Base.txt
- ✅ สร้าง embeddings สำหรับการค้นหา semantic
- ✅ ปรับปรุงการแบ่ง chunks ให้เหมาะสม

## คำถามทดสอบ

### คำถามที่ควรได้คำตอบที่ถูกต้อง:
1. **"SCORM คืออะไร"** → ควรได้คำตอบเกี่ยวกับ SCORM definition
2. **"PWA คืออะไร"** → ควรได้คำตอบเกี่ยวกับ Progressive Web App
3. **"SkillNexus รองรับ SCORM เวอร์ชันไหน"** → ควรได้ SCORM 1.2 และ 2004
4. **"ฟีเจอร์ SCORM ที่รองรับ"** → ควรได้รายการฟีเจอร์ 6 ข้อ
5. **"ราคาหลักสูตรเท่าไหร่"** → ควรได้คำตอบเกี่ยวกับราคา
6. **"มีใบประกาศนียบัตรไหม"** → ควรได้คำตอบเกี่ยวกับใบประกาศนียบัตร

## ไฟล์ที่เกี่ยวข้อง

### สคริปต์แก้ไข:
- `scripts/seed-scorm-knowledge.mjs` - เพิ่มข้อมูลใน Knowledge Base
- `scripts/import-knowledge-base.mjs` - นำเข้าข้อมูลเข้าสู่ RAG
- `scripts/fix-chatbot-knowledge.bat` - รันแก้ไขอัตโนมัติ
- `scripts/test-chatbot-fixed.mjs` - ทดสอบระบบ

### ไฟล์ที่ปรับปรุง:
- `src/lib/smart-response.ts` - ปรับปรุง SmartChatbot
- `public/Knowledge Base.txt` - ข้อมูล SCORM และ PWA

### API และ Components:
- `src/app/api/chatbot/route.ts` - API endpoint
- `src/components/chatbot/chatbot-widget.tsx` - UI component

## การตรวจสอบผลลัพธ์

### ✅ สำเร็จ:
- Chatbot ตอบคำถามเกี่ยวกับ SCORM, PWA อย่างถูกต้อง
- ได้คำตอบที่เฉพาะเจาะจง ไม่ใช่คำตอบทั่วไป
- แสดงเปอร์เซ็นต์ความแม่นยำของคำตอบ

### ❌ ยังไม่สำเร็จ:
- ยังได้คำตอบ "ขออภัย ไม่เข้าใจคำถาม"
- ไม่มีข้อมูลเฉพาะเจาะจงในคำตอบ
- ต้องตรวจสอบการนำเข้าข้อมูลอีกครั้ง

## หมายเหตุ
- ต้องรันสคริปต์แก้ไขทุกครั้งที่ reset database
- ข้อมูลใน Knowledge Base.txt สามารถแก้ไขเพิ่มเติมได้
- การทดสอบควรทำหลังจากรันสคริปต์แก้ไขเสร็จแล้ว

## การบำรุงรักษา
1. **เพิ่มข้อมูลใหม่**: แก้ไขใน `scripts/seed-scorm-knowledge.mjs`
2. **ปรับปรุง RAG**: แก้ไขใน `src/lib/rag-service.ts`
3. **ปรับปรุง Chatbot**: แก้ไขใน `src/lib/smart-response.ts`