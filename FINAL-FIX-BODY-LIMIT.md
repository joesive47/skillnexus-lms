# 🔧 แก้ไข Body Size Limit ให้หายขาด

**ปัญหา**: Body exceeded 1 MB limit (ยังไม่หาย)  
**สาเหตุ**: next.config.js ไม่ได้ผล หรือ server ไม่ได้ restart

---

## ✅ วิธีแก้ไขแบบถาวร

### 1. แก้ไข next.config.js (แก้แล้ว)

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Server Actions configuration - แก้ไข Body Size Limit
  experimental: {
    serverActions: {
      bodySizeLimit: '50mb', // เพิ่มเป็น 50MB
      allowedOrigins: ['localhost:3000', '*.vercel.app']
    }
  },
  // ... rest of config
}
```

### 2. Restart Server (ทำแล้ว)

```bash
# หยุด server (Ctrl+C)
npm run dev
```

### 3. วิธีแก้ปัญหาทันที (ถ้ายังไม่หาย)

#### วิธีที่ 1: ใช้รูปเล็กมาก
- ขนาด: < 100KB
- ความกว้าง: 400px
- Format: JPEG คุณภาพ 70%

#### วิธีที่ 2: ไม่ใส่รูปก่อน
- สร้างหลักสูตรโดยไม่ใส่รูป
- บันทึก
- กลับมา Edit แล้วค่อยใส่รูป

#### วิธีที่ 3: แยกการอัพโหลด
- สร้างหลักสูตรก่อน (ไม่ใส่รูป)
- แล้วค่อยไป Edit เพื่อใส่รูป

---

## 🚀 ทดสอบทันที

### ขั้นตอนที่ 1: รอ Server Ready
```bash
# รอจนเห็นข้อความนี้
✓ Ready in XX seconds
```

### ขั้นตอนที่ 2: Clear Browser Cache
```
1. กด F12
2. ไปที่ Application tab
3. Storage → Clear site data
4. Reload (Ctrl+Shift+R)
```

### ขั้นตอนที่ 3: ทดสอบสร้างหลักสูตร
```
1. Login: admin@example.com / admin123456
2. ไปที่: Create New Course
3. กรอกข้อมูล (ไม่ใส่รูปก่อน)
4. เพิ่มบทเรียน 1-2 บท
5. กดปุ่ม "Create Course"
```

### ขั้นตอนที่ 4: ถ้าสำเร็จ แล้วค่อยใส่รูป
```
1. ไปที่ Edit Course
2. อัพโหลดรูปเล็กๆ (< 100KB)
3. บันทึก
```

---

## 🔍 วิธีตรวจสอบว่าแก้ไขสำเร็จ

### ตรวจสอบ next.config.js
```javascript
// ต้องมี experimental.serverActions
experimental: {
  serverActions: {
    bodySizeLimit: '50mb'
  }
}
```

### ตรวจสอบ Server Logs
```bash
# ใน terminal ต้องเห็น
▲ Next.js 15.5.7
✓ Ready in XX seconds
```

### ตรวจสอบ Browser Network Tab
```
1. กด F12
2. ไปที่ Network tab
3. ลองสร้างหลักสูตร
4. ดู request ที่ fail
5. ถ้าไม่มี "413 Payload Too Large" = แก้ไขสำเร็จ
```

---

## 🎯 วิธีแก้ปัญหาเฉพาะหน้า (Emergency Fix)

### ถ้ายังไม่หาย ให้ใช้วิธีนี้:

#### 1. แก้ไขในไฟล์ course-form.tsx
```typescript
// เพิ่ม validation เข้มงวดกว่าเดิม
function handleImageChange(e: React.ChangeEvent<HTMLInputElement>) {
  const file = e.target.files?.[0]
  if (file) {
    // เปลี่ยนจาก 5MB เป็น 100KB
    if (file.size > 100 * 1024) {
      setError('Image size must be less than 100KB')
      return
    }
    // ... rest of code
  }
}
```

#### 2. หรือแยกการอัพโหลดรูป
```typescript
// ไม่ส่งรูปไปกับ form data
// แต่อัพโหลดแยกผ่าน API route
```

---

## 📊 ขนาดไฟล์ที่แนะนำ

### รูปหน้าปกหลักสูตร:
```
ขนาดที่ปลอดภัย:
- ไฟล์: < 100KB
- ความกว้าง: 400px
- ความสูง: 240px
- Format: JPEG คุณภาพ 70-80%
```

### SCORM Package:
```
ขนาดที่รองรับ:
- ไฟล์: < 10MB
- Format: .zip
- แนะนำ: ใช้ external URL แทน upload
```

---

## 🔧 วิธีลดขนาดรูป

### Online Tools:
1. **TinyPNG**: https://tinypng.com
2. **Compressor.io**: https://compressor.io
3. **Squoosh**: https://squoosh.app

### Photoshop/GIMP:
1. Resize เป็น 400x240px
2. Save for Web
3. JPEG คุณภาพ 70-80%

### Windows Paint:
1. เปิดรูป
2. Resize → 400x240
3. Save as JPEG

---

## ✅ Checklist การแก้ไข

- [x] แก้ไข next.config.js
- [x] Restart server
- [ ] Clear browser cache
- [ ] ทดสอบสร้างหลักสูตร (ไม่ใส่รูป)
- [ ] ถ้าสำเร็จ ลองใส่รูปเล็กๆ
- [ ] ถ้าไม่สำเร็จ ใช้ Emergency Fix

---

## 🎯 ผลที่คาดหวัง

### หลังแก้ไข:
- ✅ สร้างหลักสูตรได้ (ไม่ใส่รูป)
- ✅ เพิ่มบทเรียนได้
- ✅ บันทึกสำเร็จ
- ✅ อัพโหลดรูปเล็กได้ (< 100KB)

### ถ้ายังไม่ได้:
- 🔧 ใช้ Emergency Fix
- 🔧 หรือแยกการอัพโหลดรูป
- 🔧 หรือใช้ external image hosting

---

**สถานะ**: 🔧 แก้ไขแล้ว - รอ server restart แล้วทดสอบ

**จัดทำโดย**: Kiro AI Assistant  
**วันที่**: 2 กุมภาพันธ์ 2026